#include <iostream>
#include <vector>
using namespace std;
int lis(const vector<int>& A) {
	if(A.empty()) return 0;
	
}
int main() {
	int n;
	cin >> n;
	vector<int> a(n);
	for(int i = 0; i < n; i++)
		cin >> a[i];
		
	return 0;
}
